# ComfyUI-PromptEngine Dependencies
# ===================================
# 核心依赖（ComfyUI 节点运行必需）

# OpenAI API 客户端（用于调用本地 LLM）
openai>=1.0.0

# 进度条显示
tqdm>=4.65.0

# ===================================
# Step 1-3 自动化工具依赖（可选）
# 如果只使用 PromptEngine 节点，不需要这些
# 如果要运行 Step 1-3 自动化流程，需要安装

# Embedding 模型（Step 2 聚类必需）
sentence-transformers>=2.2.0

# 降维算法（Step 2 聚类必需）
umap-learn>=0.5.0

# 聚类算法（Step 2 聚类必需）
hdbscan>=0.8.33

# 数值计算（Step 2 聚类必需）
numpy>=1.24.0

# 深度学习框架（Step 2 聚类必需，二选一）
# torch>=2.0.0  # CPU 版本
# torch>=2.0.0+cu118  # GPU CUDA 版本

# Transformers 库（可选，用于加载其他模型）
# transformers>=4.30.0

# ===================================
# 说明：
# 1. 基本使用（仅 PromptEngine 节点）：
#    pip install -r requirements.txt
#    或手动安装：pip install openai tqdm
#
# 2. 完整使用（包含 Step 1-3 自动化）：
#    pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu118
#    或先安装 PyTorch，再安装其他依赖
#
# 3. ComfyUI Manager 会自动安装这些依赖
#
# ===================================
